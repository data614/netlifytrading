{
  "name": "netlifytrading",
  "private": true,
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && shx mkdir -p build/netlify/functions && shx cp index.html app.js app.css _redirects build/ && shx cp -r netlify/functions build/netlify/functions",
    "start": "npx netlify-cli@latest dev"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "rimraf": "^6.0.1",
    "shx": "^0.4.0"
  }
}
